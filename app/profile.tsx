import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ColorList from '../components/ColorList'

const Profile = () => {
  return (
    <View>
       <ColorList color="#ff0000" />
       </View>
  )
}

export default Profile

const styles = StyleSheet.create({})